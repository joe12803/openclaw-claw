name: OpenClaw Runner
on:
  workflow_dispatch:
    inputs:
      relay_api_base:
        description: 'Relay API Base URL'
        default: 'https://openclaw.994938.xyz/v1'
        required: true
      relay_api_key:
        description: 'Relay API Key'
        default: 'sk-KhbSk9pyLHkw8AzPy'
        required: true
      run_openclaw:
        description: 'Start OpenClaw Service'
        type: boolean
        default: true
      use_tunnel:
        description: 'Expose via Cloudflare Tunnel'
        type: boolean
        default: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Manual Installation
        run: |
          git clone --depth 1 https://github.com/OpenClaw/OpenClaw.git ~/openclaw-src
          cd ~/openclaw-src
          npm install -g pnpm
          pnpm install
          pnpm build

      - name: Configure OpenClaw (Absolute Alignment)
        env:
          RELAY_API_KEY: ${{ github.event.inputs.relay_api_key }}
          RELAY_API_BASE: ${{ github.event.inputs.relay_api_base }}
          GATEWAY_TOKEN: ${{ secrets.GATEWAY_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
        run: |
          # 建立标准配置目录
          mkdir -p /home/runner/.openclaw/config
          
          # 构造配置：暂时禁用 auth 以排除登录点击无反应的问题
          cat <<EOF > /home/runner/.openclaw/config/config.json
          {
            "server": { "port": 18789, "host": "0.0.0.0" },
            "gateway": {
              "auth": { "enabled": false },
              "controlUi": {
                "enabled": true,
                "allowedOrigins": ["*"],
                "public": true
              }
            },
            "providers": [
              {
                "id": "relay-api",
                "type": "openai",
                "enabled": true,
                "config": {
                  "apiKey": "${RELAY_API_KEY}",
                  "baseUrl": "${RELAY_API_BASE}"
                },
                "models": [
                  { "id": "gemini-3-flash", "name": "Gemini 3 Flash", "default": true }
                ]
              }
            ],
            "adapters": {
              "webui": { "enabled": true, "config": { "port": 18789 } },
              "telegram": {
                "enabled": true,
                "config": { "token": "${TELEGRAM_BOT_TOKEN}" }
              },
              "feishu": {
                "enabled": true,
                "config": {
                  "appId": "${FEISHU_APP_ID}",
                  "appSecret": "${FEISHU_APP_SECRET}"
                }
              }
            },
            "agents": {
              "main": { "model": "relay-api/gemini-3-flash" }
            }
          }
          EOF
          
          # 同步到所有可能的读取点
          cp /home/runner/.openclaw/config/config.json ~/openclaw-src/openclaw.json
          cp /home/runner/.openclaw/config/config.json ~/openclaw-src/config/config.json

      - name: Start OpenClaw
        run: |
          export OPENCLAW_CONFIG_DIR=/home/runner/.openclaw
          export OPENCLAW_NONINTERACTIVE=true
          export CI=true
          sudo apt-get install -y expect
          cd ~/openclaw-src
          
          # 使用绝对配置路径启动
          unbuffer ./openclaw.mjs gateway --verbose --allow-unconfigured > openclaw.log 2>&1 &
          
          echo "Waiting for OpenClaw Gateway..."
          MAX_RETRIES=40
          COUNT=0
          while [ $COUNT -lt $MAX_RETRIES ]; do
            if curl -s http://localhost:18789 > /dev/null; then
              echo "✅ OpenClaw is up with ABSOLUTE config!"
              break
            fi
            sleep 5
            COUNT=$((COUNT+1))
          done

      - name: Start Tunnel & Notify
        if: always()
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_CHAT_ID: ${{ secrets.FEISHU_CHAT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GATEWAY_TOKEN: ${{ secrets.GATEWAY_TOKEN }}
        run: |
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb
          cloudflared tunnel --url http://localhost:18789 > tunnel.log 2>&1 &
          
          echo "Waiting for Cloudflare Tunnel URL..."
          T_COUNT=0
          DETECTED=""
          while [ $T_COUNT -lt 20 ]; do
            DETECTED=$(grep -oE 'https://[a-zA-Z0-9.-]+\.trycloudflare\.com' tunnel.log | head -n 1)
            if [ -n "$DETECTED" ]; then
              echo "URL_DETECTED=$DETECTED" >> $GITHUB_ENV
              break
            fi
            sleep 3
            T_COUNT=$((T_COUNT+1))
          done
          
          if [ -n "$DETECTED" ]; then
            python3 - <<EOF
          import os, requests, json
          url = "$DETECTED"
          msg = f"🚀 OpenClaw (免登录测试版) 部署成功！\n🔗 链接: {url}\n🤖 默认模型: gemini-3-flash"
          
          tg_token = os.environ.get('TELEGRAM_BOT_TOKEN')
          tg_id = os.environ.get('TELEGRAM_CHAT_ID')
          if tg_token and tg_id:
              requests.post(f"https://api.telegram.org/bot{tg_token}/sendMessage", json={"chat_id": tg_id, "text": msg})
              webhook_url = f"{url}/telegram/webhook"
              wh_res = requests.post(f"https://api.telegram.org/bot{tg_token}/setWebhook", json={"url": webhook_url})
              print(f"Telegram webhook registered: {wh_res.json()}")
          
          fs_id = os.environ.get('FEISHU_APP_ID')
          fs_secret = os.environ.get('FEISHU_APP_SECRET')
          fs_chat_id = os.environ.get('FEISHU_CHAT_ID')
          if fs_id and fs_secret and fs_chat_id:
              id_type = "chat_id" if fs_chat_id.startswith("oc_") else "open_id"
              token_res = requests.post("https://open.feishu.cn/open-apis/auth/v3/app_access_token/internal", json={"app_id": fs_id, "app_secret": fs_secret}).json()
              app_token = token_res.get('app_access_token')
              if app_token:
                  send_url = f"https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type={id_type}"
                  headers = {"Authorization": f"Bearer {app_token}", "Content-Type": "application/json"}
                  payload = {"receive_id": fs_chat_id, "msg_type": "text", "content": json.dumps({"text": msg})}
                  requests.post(send_url, headers=headers, json=payload)
          EOF
          fi

      - name: Keep Alive
        run: sleep 21600
